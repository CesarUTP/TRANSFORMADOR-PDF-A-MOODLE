# Solo para desarrollo. La app de escritorio pide la clave al abrirla y la
# guarda cifrada en clave.dat, en su carpeta de datos (backend/credenciales.py),
# que tiene prioridad sobre este archivo.
# Copia este archivo como .env (en la raíz del proyecto o en backend/) y pon
# tu key de Google AI Studio. El .env no se versiona.
GEMINI_API_KEY=
